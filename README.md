## Get start

In the project directory run:

to launch server:
### `docker-compose up`

to lounch app:
### `npm run start`

Open [http://localhost:3000](http://localhost:3000) to view it in your browser.